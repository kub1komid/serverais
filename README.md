````
sudo apt update && sudo apt upgrade -y
````
````
sudo apt install nodejs npm -y
````
````
sudo npm install -g pm2 unrar
````
````
git clone https://github.com/kub1komid/serverais.git
````
````
pm2 startup
````
````
pm2 save
````
````
pm2 start server.js --name "srv_ais"
````
